# Configuration Evidence

This folder is reserved for the configuration files copied from the completed lab environment.

Add these after reviewing them for secrets:

- `ws01-inputs.conf`
- `ws01-outputs.conf`
- `dc01-inputs.conf`
- `dc01-outputs.conf`
- `sysmonconfig-export.xml`

Do not commit passwords, tokens, private keys, or other credentials.
